using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*  
 *  Spawner for enemy units
 *  Attatch to spawner object
 *  Should spawn mutliple random enemies at designated time intervals
 *  Different enemy ratios for each level/room?
 */
public class Spawner : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
